# Achado em 11/08/2026: este arquivo (convenção Netlify) NÃO é lido pelo
# Render Static Site — testado ao vivo, a regra abaixo nunca disparou mesmo
# depois de "Deploy live" confirmado. O mecanismo real neste serviço é o
# painel nativo "Redirects and Rewrites" do Render (Settings > Redirects/Rewrites,
# ou /static/<id>/redirects no dashboard) — não fica versionado no repo,
# só no painel. Ver carmen-runbook.md pra lista das regras reais em uso,
# incluindo o proxy de /n/* e /api/whatsapp/download/* pro backend (link de
# XML mandado pelo Martyn via WhatsApp).
#
# Mantido só por causa da regra abaixo, cuja origem/efeito real não foi
# confirmado nesta investigação — não adicionar novas regras aqui.
/index.html / 301
